#pragma once

namespace calculator {

    class Calculator {
    public:
        int sum(int first, int second);
        int minus(int first, int second);
        int divide(int first, int second);
        int times(int first, int second);
    };

}